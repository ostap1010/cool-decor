/**
 * This decorator allows execution of the method it is applied to if provided conditions are satisfied
 */
export function Precondition(conditions) : MethodDecorator {
    return function decorate(target, key: string) {

    }
}